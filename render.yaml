services:
  - type: web
    name: wanderlust
    env: node
    plan: free
    region: oregon
    buildCommand: npm install
    startCommand: node app.js
    envVars:
      - key: NODE_ENV
        value: production
      # PORT is automatically set by Render, don't add it here

