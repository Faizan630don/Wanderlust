services:
  - type: web
    name: wanderlust
    env: node
    buildCommand: npm install
    startCommand: node app.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        fromService:
          name: wanderlust
          type: web
          property: port

